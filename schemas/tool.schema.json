{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ReflexDesk Tool",
  "type": "object",
  "required": [
    "name",
    "description",
    "risk",
    "confirmation",
    "parameters"
  ],
  "properties": {
    "name": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "risk": {
      "enum": [
        "safe",
        "sensitive",
        "destructive"
      ]
    },
    "confirmation": {
      "enum": [
        "never",
        "when-ambiguous",
        "always"
      ]
    },
    "external": {
      "type": "boolean",
      "description": "Whether this tool causes side effects outside the local application boundary. Destructive + external:true maps to EXTERNAL_COMMIT."
    },
    "parameters": {
      "type": "object"
    }
  }
}
